# Źródłowy arkusz wymagań

Plik `MSSQL_alerty_Grafana_macierz_v6_complete_CPU_SQL.xlsx` jest źródłowym arkuszem, na podstawie którego powstało mapowanie wymagań do szablonu Zabbix.

Dokumentacja mapowania znajduje się w `docs/02-excel-mapping.md` oraz w katalogu `docs/excel-mapping/`.
