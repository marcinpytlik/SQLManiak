# Raport porządkowania SQL

- Zmieniono kolejność plików.
- `99_Useful_Queries.sql` jest plikiem końcowym.
- Dodano `98_Verify_Installation.sql`.
- Tabele i samodzielne indeksy zabezpieczono testem istnienia.
- Procedury i widoki pozostają oparte na `CREATE OR ALTER`.
- Ujednolicono konwencję `[schemat].[Obiekt]`.
- Ujednolicono schemat backupów do `[backup]`.

## Pozostałe niekwalifikowane odwołania

Brak.
