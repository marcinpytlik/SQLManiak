# 02 – TRUNCATE vs DROP

**Cel:** zademonstrować, że `TRUNCATE` czyści dane, ale **zostawia strukturę**, a `DROP` usuwa **cały obiekt** (dane + schemat).

Uruchom `demo.sql` w całości lub krok po kroku.
