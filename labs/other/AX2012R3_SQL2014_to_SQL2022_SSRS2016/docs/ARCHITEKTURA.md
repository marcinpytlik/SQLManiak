# Architektura docelowa

A (SQL 2014) → wyłączamy po migracji (bez SSRS).  
B (SQL 2022) → bazy AX + (opcjonalnie) ReportServer.  
C (SSRS 2016) → jedyny serwer raportów AX, z AX Reporting Extensions.
