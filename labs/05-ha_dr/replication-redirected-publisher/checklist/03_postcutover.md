# Po przełączeniu

- [ ] `sql/09_monitoring.sql`: undistributed cmds, tracer tokens, historia jobów, `sp_replcounters`.
- [ ] Przegląd błędów w msdb (Operator/Alerty jeśli używane).
- [ ] Weryfikacja uprawnień loginów agentów na C.
- [ ] Dokumentacja zmian (kto/co/kiedy).
- [ ] Plan późniejszej migracji dystrybucji z A → C (osobny change).
